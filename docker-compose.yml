version: '3.8'

services:
  meme-generator:
    build:
      context: .
      dockerfile: Dockerfile
      # 构建时使用 Docker Build Secrets（如果需要）：
      # docker buildx build --secret id=BAIDU_TRANS_APPID,src=./secrets/baidu_trans_appid.txt --secret id=BAIDU_TRANS_APIKEY,src=./secrets/baidu_trans_apikey.txt .
    container_name: meme-generator
    ports:
      - "8080:8080"
    volumes:
      - ./data/memes:/data/memes
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
      - LOAD_BUILTIN_MEMES=true
      - MEME_DIRS=["/data/memes"]
      - MEME_DISABLED_LIST=[]
      - GIF_MAX_SIZE=10.0
      - GIF_MAX_FRAMES=100
      - LOG_LEVEL=INFO
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      # 通过环境变量在运行时传递密钥（可选）
      # - BAIDU_TRANS_APPID=your_appid
      # - BAIDU_TRANS_APIKEY=your_apikey
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8080/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    networks:
      - meme-network

networks:
  meme-network:
    driver: bridge

volumes:
  meme-data:
    driver: local